version: '{build}'

before_build:
  - nuget restore
  
build:
  publish_nuget: true
  publish_nuget_symbols: true

image: Visual Studio 2017

deploy:
  - provider: Environment
    name: nuget-telepathy
    artifact: /.*\.nupkg/